---
phase: 03-integrate-skills
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .planning/phases/03-integrate-skills/VERIFICATION-CHECKLIST.md
autonomous: true
requirements:
  - INTEG-03

must_haves:
  truths:
    - "Every section heading from every original file is accounted for — present in either a shared file or the trimmed domain-specific file"
    - "No instruction, example, or edge case from the original files is lost"
    - "The checklist is a standalone reviewable artifact"
  artifacts:
    - path: ".planning/phases/03-integrate-skills/VERIFICATION-CHECKLIST.md"
      provides: "Section-level audit proving zero content loss"
      contains: "| Section |"
  key_links:
    - from: ".planning/phases/03-integrate-skills/VERIFICATION-CHECKLIST.md"
      to: "skills/shared/"
      via: "section-by-section mapping"
      pattern: "shared/"
---

<objective>
Build a section-level verification checklist confirming zero logic loss: every instruction, example, and edge case from the original files is present in either a shared file or the trimmed domain-specific file.

Purpose: Satisfies INTEG-03 — formal proof that the refactor preserved all content.
Output: VERIFICATION-CHECKLIST.md in the phase directory.
</objective>

<execution_context>
@C:/Users/dan.munteanu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dan.munteanu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-integrate-skills/03-CONTEXT.md
@.planning/phases/03-integrate-skills/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build section-level verification checklist</name>
  <files>.planning/phases/03-integrate-skills/VERIFICATION-CHECKLIST.md</files>
  <read_first>
    skills/uipath-coded-workflows/references/ui-automation-guide.md
    skills/uipath-rpa-workflows/references/ui-automation-guide.md
    skills/uipath-coded-workflows/references/coding-guidelines.md
    skills/uipath-rpa-workflows/references/validation-and-fixing.md
    skills/uipath-coded-workflows/references/uip-guide.md
    skills/uipath-rpa-workflows/references/cli-reference.md
    skills/uipath-coded-workflows/SKILL.md
    skills/uipath-rpa-workflows/SKILL.md
    skills/shared/uia-prerequisites.md
    skills/shared/uia-configure-target-workflows.md
    skills/shared/uia-debug-workflow.md
    skills/shared/uia-selector-recovery.md
    skills/shared/uia-multi-step-flows.md
    skills/shared/validation-loop.md
    skills/shared/cli-reference.md
  </read_first>
  <action>
Create `.planning/phases/03-integrate-skills/VERIFICATION-CHECKLIST.md` with the following structure:

**Header:** `# Phase 3 Verification Checklist — Zero Logic Loss` with date and purpose.

**For each of the 8 modified files**, create a table with these columns:
- `Section` — every section heading from the ORIGINAL file (use git to retrieve pre-edit content: `git show HEAD~1:path/to/file` or `git diff HEAD~1 -- path/to/file`)
- `Location` — where this section's content now lives: `stays` (still in domain file), `shared/{filename}` (moved to shared), `pointer added` (heading kept, content replaced with pointer)
- `Status` — checkmark or issue flag

**Process for each file:**
1. Use `git show HEAD~1:{filepath}` to get the original content (before Plan 01 edits)
2. Extract all section headings from the original
3. For each heading, determine: was it kept in the domain file (paradigm-specific), moved to a shared file (pointer replaces content), or split (some content kept, some pointed)?
4. Verify the shared file actually contains the corresponding content by reading it
5. Flag any section where content appears to be missing from both locations

**After all file tables**, add a summary section:
- Total sections audited
- Sections in domain files (paradigm-specific, unchanged)
- Sections pointed to shared files
- Sections with issues (if any)
- Verdict: PASS or FAIL with explanation

Use `git diff` to cross-reference what was removed from domain files vs what exists in shared files.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-integrate-skills/VERIFICATION-CHECKLIST.md && grep -c "| Section" .planning/phases/03-integrate-skills/VERIFICATION-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/03-integrate-skills/VERIFICATION-CHECKLIST.md
    - File contains a table for each of the 8 modified files (grep for `## skills/uipath-coded` returns at least 3 matches, `## skills/uipath-rpa` returns at least 3 matches)
    - File contains a `## Summary` section
    - File contains a verdict line with either `PASS` or `FAIL`
    - Every section heading from the original files appears in a table row
  </acceptance_criteria>
  <done>VERIFICATION-CHECKLIST.md exists as a standalone artifact with section-level mapping for all 8 files, total sections audited, and a PASS/FAIL verdict confirming zero logic loss.</done>
</task>

</tasks>

<verification>
- VERIFICATION-CHECKLIST.md covers all 8 files modified in Plan 01
- Every original section heading is accounted for
- Summary includes clear PASS/FAIL verdict
</verification>

<success_criteria>
- Standalone VERIFICATION-CHECKLIST.md exists and is human-reviewable
- All sections from all original files mapped to their current location
- No content flagged as missing (or if flagged, explained)
- Verdict is PASS
</success_criteria>

<output>
After completion, create `.planning/phases/03-integrate-skills/03-02-SUMMARY.md`
</output>
