---
phase: 01-extract-uiautomation-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/shared/uia-prerequisites.md
  - skills/shared/uia-servo-workflows.md
  - skills/shared/uia-multi-step-flows.md
autonomous: true
requirements: [SHARED-01, SHARED-02, SHARED-05]

must_haves:
  truths:
    - "A shared prerequisites file contains the package version check, upgrade commands, and fallback warning with no C#/XAML specifics"
    - "A shared servo workflows file contains uia-configure-target usage, skill discovery glob, invocation syntax, Do-NOT warnings, and indication fallback commands with no paradigm-specific post-indication retrieval"
    - "A shared multi-step flows file contains the complete-then-advance rule, servo workflow steps, Do-NOT-use-run-file warning, and servo-vs-UIA refs warning with no code/XAML word in the final sentence"
  artifacts:
    - path: "skills/shared/uia-prerequisites.md"
      provides: "Package version requirement, check command, upgrade command, fallback warning"
      contains: "26.3.1-beta.11555873"
    - path: "skills/shared/uia-servo-workflows.md"
      provides: "uia-configure-target intro, invocation, Do NOT warnings, indication fallback commands"
      contains: "uia-configure-target"
    - path: "skills/shared/uia-multi-step-flows.md"
      provides: "Complete-then-advance rule, servo capture-advance cycle, run-file warning"
      contains: "Complete-then-advance"
  key_links:
    - from: "skills/shared/uia-prerequisites.md"
      to: "skills/shared/uia-servo-workflows.md"
      via: "fallback reference"
      pattern: "indication tools"
    - from: "skills/shared/uia-multi-step-flows.md"
      to: "skills/shared/uia-servo-workflows.md"
      via: "cross-reference to servo workflows"
      pattern: "uia-servo-workflows"
---

<objective>
Create the three shared UiAutomation reference files that have content duplicated across BOTH skill guides: prerequisites, servo/indication workflows, and multi-step UI flow patterns.

Purpose: Extract verbatim-identical content from both ui-automation-guide.md files into shared location so future edits happen once.
Output: Three markdown files under skills/shared/ containing paradigm-neutral shared content.
</objective>

<execution_context>
@C:/Users/dan.munteanu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dan.munteanu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-extract-uiautomation-content/01-CONTEXT.md
@.planning/phases/01-extract-uiautomation-content/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared prerequisites file (SHARED-01)</name>
  <files>skills/shared/uia-prerequisites.md</files>
  <read_first>
    - skills/uipath-coded-workflows/references/ui-automation-guide.md (lines 10-24, Prerequisites section)
    - skills/uipath-rpa-workflows/references/ui-automation-guide.md (lines 9-23, Prerequisites section)
    - .planning/phases/01-extract-uiautomation-content/01-RESEARCH.md (Content Map, uia-prerequisites.md section)
  </read_first>
  <action>
Create `skills/shared/` directory and `skills/shared/uia-prerequisites.md`.

Content to include (extracted from both source files where the text is verbatim-identical):

1. **Title:** `# UiAutomation Prerequisites`

2. **Package requirement line:** `**Required package:** \`UiPath.UIAutomation.Activities\``

3. **Version check paragraph** (verbatim from both files, identical):
   "The `uip rpa uia` subcommands (snapshot, selector-intelligence, object-repository) used by `uia-configure-target` require **`UiPath.UIAutomation.Activities` >= 26.3.1-beta.11555873**. Before configuring any target, check the installed version in `project.json` under `dependencies`."

4. **Upgrade prompt paragraph** (verbatim): "If the installed version is below the minimum, ask the user whether to upgrade:"

5. **Code block** (verbatim-identical in both):
   ```bash
   uip rpa get-versions --package-id UiPath.UIAutomation.Activities --project-dir "$PROJECT_DIR" --format json

   # If user approves the upgrade:
   uip rpa install-or-update-packages --packages '[{"id": "UiPath.UIAutomation.Activities", "version": "26.3.1-beta.11555873"}]' --project-dir "$PROJECT_DIR" --format json
   ```

6. **Bridge sentence** (generic — replaces both "Fallback: Raw Indication Commands" and "Low-Level Indication Tools" links):
   "If the user declines, warn that `uip rpa uia` commands will fail and fall back to the indication tools (see [uia-servo-workflows.md](uia-servo-workflows.md) for fallback commands)."

Do NOT include:
- The opening framing line about `uiAutomation` service or XAML activities (paradigm-specific)
- The `> **For full API details:**` line (differs between coded/rpa paths)
- The `**Service accessor:**` line (coded-only)
  </action>
  <verify>
    <automated>grep -c "26.3.1-beta.11555873" skills/shared/uia-prerequisites.md | grep -q "2" && echo "PASS: version appears twice (check + install)" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - skills/shared/uia-prerequisites.md exists
    - File contains "# UiAutomation Prerequisites"
    - File contains "uip rpa get-versions --package-id UiPath.UIAutomation.Activities"
    - File contains "uip rpa install-or-update-packages"
    - File contains "26.3.1-beta.11555873" (appears in both the version check paragraph and the install command)
    - File contains link to "uia-servo-workflows.md" for fallback
    - File does NOT contain "ObjectRepository.cs", "IUiAutomationAppService", "uiAutomation", "XAML", ".objects/", "Descriptors."
  </acceptance_criteria>
  <done>Shared prerequisites file exists with package version check, upgrade commands, and generic fallback reference. No paradigm-specific content present.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared servo workflows file (SHARED-02)</name>
  <files>skills/shared/uia-servo-workflows.md</files>
  <read_first>
    - skills/uipath-coded-workflows/references/ui-automation-guide.md (lines 83-150, uia-configure-target + Fallback section)
    - skills/uipath-rpa-workflows/references/ui-automation-guide.md (lines 48-121, Configuring Targets + Low-Level Indication Tools)
    - .planning/phases/01-extract-uiautomation-content/01-RESEARCH.md (uia-servo-workflows.md extraction plan)
  </read_first>
  <action>
Create `skills/shared/uia-servo-workflows.md`.

Content sections to include:

1. **Title:** `# Servo and Indication Workflows`

2. **uia-configure-target intro** (verbatim-identical in both files):
   "**Always use the `uia-configure-target` skill** to create or find targets in the Object Repository. This skill handles the full flow: snapshot capture, element discovery, selector generation, selector improvement, and OR registration."

3. **Skill discovery glob** (verbatim-identical):
   The UIA activity-docs paragraph with the Glob pattern `**/*.md` and the disclaimer about reference docs vs slash commands. Copy verbatim from either source.

4. **Invocation syntax** (verbatim-identical):
   - Window + element: `--window <description> --element <description>`
   - Window only: `--window <description>`

5. **Skill behavior paragraph** (verbatim-identical):
   "The skill will search the Object Repository for existing matches before creating new entries, generate selectors from the live application tree, and register everything in the OR."

6. **Bridge sentence** after skill behavior: "After completion, retrieve the target references for your workflow."
   (This replaces coded's "re-read ObjectRepository.cs to get the descriptor paths" and RPA's "return the XAML snippet to use directly".)

7. **"Do NOT" warnings** (from coded file lines 97-99, not present in RPA but applicable to both):
   - "**Do NOT manually call low-level `uip rpa uia` CLI commands** (`snapshot capture`, `snapshot filter`, `selector-intelligence get-default-selector`) to build selectors outside of the skill flow. These are internal tools used *by* the skill — calling them directly skips selector improvement and OR registration, producing fragile selectors that aren't tracked in the project."
   - "**Do NOT launch the target application before running `uia-configure-target`.** The skill's first steps capture the top-level window tree and search for the app. Only if the app is not found in the window list should you launch it — and then re-run the capture. Launching preemptively creates duplicate instances and risks targeting the wrong window."

8. **Indication fallback section** with heading `## Indication Fallback Commands`
   (Neutral heading replacing coded's "Fallback: Raw Indication Commands" and RPA's "Low-Level Indication Tools (Alternative)")

   Include the intro paragraph (verbatim-identical in both), then the two code blocks:
   ```bash
   uip rpa indicate-application --name "<ScreenName>" --description "<ScreenDescription>" --project-dir "<PROJECT_DIR>" --format json
   uip rpa indicate-element --name "<ElementName>" --description "<ElementDescription>" --parent-id "<screen-reference>" --activity-class-name "<ActivityType>" --project-dir "<PROJECT_DIR>" --format json
   ```

   Then: "Both commands return the OR reference ID."

   Then the XAML retrieval commands (identical in both):
   ```bash
   uip rpa uia object-repository get-screen-xaml --reference-id "<screen-reference>"
   uip rpa uia object-repository get-element-xaml --reference-id "<element-reference>"
   ```

   Bridge sentence: "After indication, retrieve the target references from your project's Object Repository to use in your workflow."
   (Replaces coded's "re-reading ObjectRepository.cs, or retrieve the ready-to-use XAML snippets" and RPA's "retrieve the ready-to-use XAML snippets".)

Do NOT include:
- "All steps must complete — do not stop after getting a raw selector." (coded-specific framing about Step 3)
- "After completion, re-read `ObjectRepository.cs` to get the descriptor paths." (coded-specific)
- "return the XAML snippet to use directly" (RPA-specific)
- The "Applying Targets to XAML" subsection (RPA-specific)
- The "Capturing New UI Targets" section (RPA-specific, partially overlapping with fallback — flagged in review doc)
- Any mention of `Descriptors.`, `ObjectRepository.cs`, `.objects/`, `NApplicationCard`, XAML syntax
  </action>
  <verify>
    <automated>grep -c "uia-configure-target" skills/shared/uia-servo-workflows.md | grep -qE "^[2-9]|^[1-9][0-9]" && grep -q "indicate-application" skills/shared/uia-servo-workflows.md && grep -q "Do NOT" skills/shared/uia-servo-workflows.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - skills/shared/uia-servo-workflows.md exists
    - File contains "# Servo and Indication Workflows"
    - File contains "uia-configure-target" (multiple occurrences)
    - File contains the Glob pattern `**/*.md`
    - File contains "--window" and "--element" invocation syntax
    - File contains two "Do NOT" warning paragraphs
    - File contains "## Indication Fallback Commands" heading
    - File contains "indicate-application" and "indicate-element" commands
    - File contains "get-screen-xaml" and "get-element-xaml" commands
    - File does NOT contain "ObjectRepository.cs", "Descriptors.", "IDisposable", "UiTargetApp", ".objects/", "NApplicationCard", "xmlns:"
  </acceptance_criteria>
  <done>Shared servo workflows file covers uia-configure-target usage, Do-NOT warnings, and indication fallback with generic bridge sentences at paradigm boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Create shared multi-step flows file (SHARED-05)</name>
  <files>skills/shared/uia-multi-step-flows.md</files>
  <read_first>
    - skills/uipath-coded-workflows/references/ui-automation-guide.md (lines 101-128, Multi-Step UI Flows section)
    - skills/uipath-rpa-workflows/references/ui-automation-guide.md (lines 68-95, Multi-Step UI Flows section)
    - .planning/phases/01-extract-uiautomation-content/01-RESEARCH.md (uia-multi-step-flows.md extraction plan)
  </read_first>
  <action>
Create `skills/shared/uia-multi-step-flows.md`.

Content sections (the Multi-Step UI Flows section is verbatim-identical in both sources except the final sentence):

1. **Title:** `# Multi-Step UI Flows`

2. **Intro paragraph** (verbatim from either source):
   "Some UI elements only become visible after interacting with earlier elements (e.g., a compose form appears after clicking "New mail", a confirmation dialog appears after submitting). Since `uia-configure-target` works from the current screen state, you need to **advance the application to each state** before capturing its elements."

3. **CRITICAL blockquote** (verbatim-identical in both):
   The Complete-then-advance blockquote starting with "> **CRITICAL: Complete-then-advance.**" — copy the entire blockquote including the "Do NOT use servo to 'test'" paragraph. This is identical in both files.

4. **WARNING blockquote** (verbatim-identical in both):
   The servo-vs-UIA refs warning starting with "> **WARNING: Servo refs and UIA snapshot refs are independent numbering systems.**" — copy verbatim.

5. **Numbered procedure** (verbatim-identical in both):
   "Use the `servo` CLI to interact with already-configured targets and advance the UI, then run `uia-configure-target` again for the newly visible elements:"
   Steps 1-4 with the servo code block. Copy verbatim from either source.

6. **run-file warning** (verbatim-identical):
   "**Do NOT use `uip rpa run-file` with partial workflows to advance UI state** — the workflow lifecycle may close the target application when execution ends. Servo is stateless: it clicks/types and leaves the app in the resulting state."

7. **Bridge sentence** (generic replacement for the differing final line):
   "After all targets are captured, build the full workflow in one pass using all the collected OR references."
   (Drops "code"/"XAML" — coded says "code", RPA says "XAML".)

8. **Cross-reference:** "See also: [uia-servo-workflows.md](uia-servo-workflows.md) for the `uia-configure-target` skill details and indication fallback commands."

Do NOT include: any mention of "code" or "XAML" in the final sentence. No paradigm-specific terms anywhere.
  </action>
  <verify>
    <automated>grep -q "Complete-then-advance" skills/shared/uia-multi-step-flows.md && grep -q "Do NOT use.*run-file" skills/shared/uia-multi-step-flows.md && grep -q "servo snapshot" skills/shared/uia-multi-step-flows.md && ! grep -qi "ObjectRepository\|Descriptors\.\|\.objects/" skills/shared/uia-multi-step-flows.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - skills/shared/uia-multi-step-flows.md exists
    - File contains "# Multi-Step UI Flows"
    - File contains "Complete-then-advance" in a CRITICAL blockquote
    - File contains the servo-vs-UIA refs WARNING blockquote
    - File contains "servo targets", "servo snapshot", "servo click" in the numbered steps
    - File contains "Do NOT use `uip rpa run-file`" warning
    - File final build sentence says "build the full workflow" (NOT "code" or "XAML")
    - File contains cross-reference link to "uia-servo-workflows.md"
    - File does NOT contain "ObjectRepository.cs", "Descriptors.", ".objects/", "XAML", the word "code" in the final build sentence
  </acceptance_criteria>
  <done>Shared multi-step flows file contains the complete-then-advance pattern, servo workflow steps, all warnings, and generic bridge sentence. No paradigm-specific content.</done>
</task>

</tasks>

<verification>
After all three files are created, run these checks:

1. All three files exist under skills/shared/
2. No paradigm-specific leakage — grep across all shared files for forbidden terms:
   ```bash
   grep -rn "ObjectRepository\.cs\|Descriptors\.\|IDisposable\|UiTargetApp\|TargetAnchorable\|NApplicationCard\|xmlns:\|\.objects/" skills/shared/
   ```
   Expected: no matches.
3. Each file is self-contained with a title heading and no broken internal links.
</verification>

<success_criteria>
- Three shared files exist at skills/shared/uia-prerequisites.md, skills/shared/uia-servo-workflows.md, skills/shared/uia-multi-step-flows.md
- Each file contains only paradigm-neutral content verified by grep for forbidden terms
- Bridge sentences appear at every paradigm divergence point
- Cross-references between shared files use relative links
</success_criteria>

<output>
After completion, create `.planning/phases/01-extract-uiautomation-content/01-01-SUMMARY.md`
</output>
